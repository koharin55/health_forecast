<div class="w-full">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-3xl font-bold text-gray-800">健康記録一覧</h1>
    <%= link_to "新しい記録を追加", new_health_record_path, class: "bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-lg font-semibold" %>
  </div>

  <div class="bg-white rounded-lg shadow-md overflow-hidden">
    <% if @health_records.any? %>
      <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200">
          <thead class="bg-gray-50">
            <tr>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">日付</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">体重</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">睡眠</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">運動</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">歩数</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">心拍数</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">体調</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">操作</th>
            </tr>
          </thead>
          <tbody class="bg-white divide-y divide-gray-200">
            <% @health_records.each do |record| %>
              <tr class="hover:bg-gray-50">
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 font-medium">
                  <%= record.recorded_at.strftime("%Y-%m-%d") %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                  <%= record.weight.present? ? "#{record.weight} kg" : "-" %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                  <%= record.sleep_hours.present? ? "#{record.sleep_hours}h" : "-" %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                  <%= record.exercise_minutes.present? ? "#{record.exercise_minutes}分" : "-" %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                  <%= record.steps.present? ? number_with_delimiter(record.steps) : "-" %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                  <%= record.heart_rate.present? ? "#{record.heart_rate} bpm" : "-" %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                  <% if record.mood.present? %>
                    <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full
                      <%= case record.mood
                          when 5 then 'bg-green-100 text-green-800'
                          when 4 then 'bg-blue-100 text-blue-800'
                          when 3 then 'bg-yellow-100 text-yellow-800'
                          when 2 then 'bg-orange-100 text-orange-800'
                          when 1 then 'bg-red-100 text-red-800'
                          end %>">
                      <%= record.mood %>/5
                    </span>
                  <% else %>
                    -
                  <% end %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium space-x-2">
                  <%= link_to "詳細", record, class: "text-blue-600 hover:text-blue-900" %>
                  <%= link_to "編集", edit_health_record_path(record), class: "text-green-600 hover:text-green-900" %>
                  <%= button_to "削除", record, method: :delete,
                      class: "inline text-red-600 hover:text-red-900",
                      form: { class: "inline" },
                      data: { turbo_confirm: "本当に削除しますか？" } %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    <% else %>
      <p class="text-gray-600 text-center py-12">
        まだ記録がありません。<%= link_to "最初の記録を追加", new_health_record_path, class: "text-blue-600 hover:text-blue-900 font-semibold" %>しましょう！
      </p>
    <% end %>
  </div>
</div>
